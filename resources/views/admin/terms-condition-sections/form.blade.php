@extends('layouts.admin')
@section('page-title', $section->exists ? 'Edit Terms Section' : 'Add Terms Section')
@section('content')
<div class="admin-page-head"><div><h2 class="admin-page-title">{{ $section->exists ? 'Edit' : 'Add' }} Terms Section</h2><p class="admin-page-subtitle">Use the visual editor below; no HTML or client code is needed.</p></div></div><form method="POST" action="{{ $section->exists ? route('admin.terms-condition-sections.update',$section) : route('admin.terms-condition-sections.store') }}">@csrf @if($section->exists)@method('PUT')@endif<div class="admin-form-grid"><div class="form-card"><div class="form-card-body"><div class="field-group"><label class="field-label">Title</label><input class="field-input" name="title" value="{{ old('title',$section->title) }}" required></div><div class="field-group"><label class="field-label">Subtitle</label><input class="field-input" name="subtitle" value="{{ old('subtitle',$section->subtitle) }}"></div><div class="field-group"><label class="field-label">Bootstrap Icon Class</label><input class="field-input" name="icon_class" value="{{ old('icon_class',$section->icon_class) }}"></div><div class="field-group"><label class="field-label">Content</label><textarea id="termsContentEditor" class="field-input" style="height:auto" rows="12" name="content" required>{{ old('content',$section->content) }}</textarea><p class="field-hint">Use headings, paragraphs, bold text and lists from the editor toolbar.</p></div></div></div><div class="form-card"><div class="form-card-body"><div class="field-group"><label class="field-label">Display Order</label><input class="field-input" type="number" name="sort_order" value="{{ old('sort_order',$section->sort_order) }}"></div><label class="role-checkbox-item {{ old('status',$section->status ?? true) ? 'checked' : '' }}"><input class="role-checkbox" type="checkbox" name="status" value="1" @checked(old('status',$section->status ?? true))><div class="check-icon"></div><span class="checkbox-text">Published</span></label></div></div></div><div class="form-actions"><button class="btn-primary">Save Section</button><a class="btn-ghost" href="{{ route('admin.terms-condition-sections.index') }}">Cancel</a></div></form>
@endsection
@section('scripts') @parent <script>ClassicEditor.create(document.querySelector('#termsContentEditor')).catch(function(error){console.error(error);});</script> @endsection
